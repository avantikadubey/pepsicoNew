/**
 * Override the default store of the theme
 */
const reducers = null; // List of all reducers composed together
const enhancers = null; // List of all enhancers applied to the redux store
const watchers = []; // Saga configuration of watchers
const middlewares = null; // Saga or any other middleware configuration
const initStore = (store) => {
  // no op
};
export { reducers, enhancers, watchers, middlewares, initStore };
